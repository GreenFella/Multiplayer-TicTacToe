

let player

function rect1js() {
    document.getElementById("test1").innerHTML = "YOU CLICKED!";
}

function findPlayerButton() {

  document.getElementById("red").innerHTML = "YOU CLICKED!";
/*
  const ws = new WebSocket('ws://20.248.161.0:8081');

ws.on('open', function open() {
  ws.send('something');
});

ws.on('message', function message(data) {
  console.log('received: %s', data);
});

  
    fetch('/findplayer', {
      method: 'POST',
      headers: {
      'Accept': 'application/json',
      'Content-Type': 'application/json'
      },
    })
    .then(res => {
      if (res.ok) return res.json()
    })
    .then(response => {
      if (response == "circles") {
        let player = circles
      }
        else if (response == "exes") {
          let player = exes
      }
    })
    */
  }

 